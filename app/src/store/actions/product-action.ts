import { API_ENDPOINT } from "../../configs/constants"

export const addProd = async (data: any, token: string | null) => {
    const url = API_ENDPOINT + '/product'
    try {

    } catch (error) {

    }
}

export const getProdById = async (
    id: string | null,
    token: string | null
) => { }

export const getUserProd = async (
    id: string | null,
    token: string | null
) => { }

export const updateProd = async (id: string, token: string | null) => { }

export const deleteProd = async (id: string, token: string | null) => { }